<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>NickERP.Perf.Tests</RootNamespace>
    <!--
      This project is the Phase V perf-test harness. It is NOT a unit-test project.
      'dotnet test' discovers ZERO tests here by design; perf scenarios run only
      when explicitly invoked via 'dotnet run' with a scenario name.

      Suite total stays at 1099 of 1099 passing tests; this project contributes 0.
    -->
    <IsPackable>false</IsPackable>
    <IsTestProject>false</IsTestProject>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Sprint 58 — replaced NBomber 6.1.0 (commercial subscription per
      SEC-DEP-3) + NBomber.Http 6.0.0 with a homegrown in-tree runner at
      Runner/. NBomber.Contracts (Apache-2.0) was also dropped; the few
      types we used (Scenario / LoadSimulation / NodeStats) had small
      enough surfaces that owning them cleanly was worth it.

      The replacement primitives live at:
        Runner/NickPerfScenario.cs   — typed scenario shape
        Runner/NickPerfRunner.cs     — rate-based scheduler
        Runner/NickPerfStats.cs      — p50/p95/p99 percentiles
        Runner/NickPerfReport.cs     — markdown report
        Runner/Http/NickPerfHttp.cs  — direct HttpClient helpers

      License posture: the project now has NO non-allowlisted dependencies.
    -->
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.EnvironmentVariables" Version="10.0.0" />
    <!--
      Sprint 52 / FU-perf-auth-mocking-decision (Sprint 30) — JWT signing for the
      mock auth seam. The mock-JWT path (Auth/MockJwtBearerHandler.cs) signs
      CF-Access-shaped JWTs against a per-run key pair; the matching API-side
      JWKS-mock is wired by Phase V follow-up. Real-CF-Access spot-check path
      uses an out-of-band token via NICKERP_PERF_BEARER_TOKEN env var (no
      package needed for that path). Same major version Identity uses.
    -->
    <PackageReference Include="System.IdentityModel.Tokens.Jwt" Version="8.16.0" />
    <PackageReference Include="Microsoft.IdentityModel.Tokens" Version="8.16.0" />
  </ItemGroup>

  <ItemGroup>
    <None Include="appsettings.json" CopyToOutputDirectory="PreserveNewest" />
    <None Include="README.md" />
  </ItemGroup>

</Project>
