<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\platform\NickERP.Platform.Audit\NickERP.Platform.Audit.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Audit.Database\NickERP.Platform.Audit.Database.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Plugins\NickERP.Platform.Plugins.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Identity.Api\NickERP.Platform.Identity.Api.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Tenancy\NickERP.Platform.Tenancy.csproj" />
    <!-- Sprint 18 — TenantLifecycleService + TenantPurgeOrchestrator tests. -->
    <ProjectReference Include="..\..\platform\NickERP.Platform.Tenancy.Database\NickERP.Platform.Tenancy.Database.csproj" />
    <!-- Sprint 21 — Email senders + InviteService tests. -->
    <ProjectReference Include="..\..\platform\NickERP.Platform.Email\NickERP.Platform.Email.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Identity.Database\NickERP.Platform.Identity.Database.csproj" />
    <!-- Sprint 29 — module registry / per-tenant settings / shared chrome
         all live in apps/portal/Services/Modules + platform/NickERP.Platform.Web.Shared.
         The portal project ref drags Web.Shared along transitively. -->
    <ProjectReference Include="..\..\platform\NickERP.Platform.Web.Shared\NickERP.Platform.Web.Shared.csproj" />
    <ProjectReference Include="..\..\apps\portal\NickERP.Portal.csproj" />
    <!-- Sprint 52 / FU-perf-tenant-data-shape — smoke test for the perf-seed
         tool. The reference does not pull anything heavy; the seeder is a
         tiny console-grade class that drives EF directly. -->
    <ProjectReference Include="..\..\tools\perf-seed\NickERP.Tools.PerfSeed.csproj" />
    <!-- Sprint 14 / B-queues — WorkItemStateMachine unit tests + PostgresQueue
         integration tests. Both projects expose InternalsVisibleTo this assembly. -->
    <ProjectReference Include="..\..\platform\NickERP.Platform.Queueing\NickERP.Platform.Queueing.csproj" />
    <ProjectReference Include="..\..\platform\NickERP.Platform.Queueing.Database\NickERP.Platform.Queueing.Database.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Authorities.CustomsGh\NickERP.Inspection.Authorities.CustomsGh.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.ExternalSystems.IcumsGh\NickERP.Inspection.ExternalSystems.IcumsGh.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.ExternalSystems.Mock\NickERP.Inspection.ExternalSystems.Mock.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Inference.Mock\NickERP.Inspection.Inference.Mock.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Inference.OCR.ContainerNumber\NickERP.Inspection.Inference.OCR.ContainerNumber.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Inference.OnnxRuntime\NickERP.Inspection.Inference.OnnxRuntime.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Scanners.Ase\NickERP.Inspection.Scanners.Ase.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Scanners.FS6000\NickERP.Inspection.Scanners.FS6000.csproj" />
    <ProjectReference Include="..\..\modules\inspection\plugins\NickERP.Inspection.Scanners.Mock\NickERP.Inspection.Scanners.Mock.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- G1-3 system-context tests open real Postgres connections to nickerp_platform / nickerp_inspection
         under the nscim_app role to exercise the new RLS opt-in policy on audit.events. -->
    <PackageReference Include="Npgsql" Version="10.0.2" />
    <!-- Sprint 8 P3 — AuditNotificationProjectorTests use the EF in-memory provider to drive the
         projector through one tick deterministically without a Postgres dependency. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" Version="10.0.7" />
    <!-- Sprint 14 / B-queues — Testcontainers spins a throwaway postgres:17-alpine
         per integration-test fixture so the queueing claim/lease/DLQ paths run
         against real Postgres without depending on a developer's localhost cluster.
         Tests are gated on Docker availability + auto-skip when the daemon isn't
         reachable so CI without Docker passes silently (matches the
         NICKSCAN_DB_PASSWORD-skip convention used by the existing live-DB suites). -->
    <PackageReference Include="Testcontainers.PostgreSql" Version="3.10.0" />
    <!-- Sprint 35 / B8.2 — bunit page-render coverage for the
         portal admin pages (FeatureFlags + TenantSettings). -->
    <PackageReference Include="bunit" Version="1.36.0" />
  </ItemGroup>

</Project>
